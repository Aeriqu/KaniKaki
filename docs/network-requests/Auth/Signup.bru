meta {
  name: Signup
  type: graphql
  seq: 1
}

post {
  url: http://localhost/api
  body: graphql
  auth: none
}

body:graphql {
  mutation {
  	signup(identifier: "testUser", credential: "testPassword") {
  		token
  	}
  }
}

script:post-response {
  let data = res.body.data;
  bru.setEnvVar("AUTH_TOKEN", data.signup.token);
}
